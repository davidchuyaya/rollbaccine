# File used by xfstests

# TEST_DEV and TEST_DIR don't matter, because none of the tests use them
export TEST_DEV=/dev/sdb1
export TEST_DIR=/mnt/test

# These should point to the rollbaccine primary
export SCRATCH_DEV=/dev/mapper/rollbaccine1
export SCRATCH_MNT=/mnt/newfs